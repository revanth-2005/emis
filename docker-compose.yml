services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ermisglobal-web
    ports:
      - "4321:4321"
    environment:
      - HOST=0.0.0.0
      - PORT=4321
      - NODE_ENV=production
    restart: always
