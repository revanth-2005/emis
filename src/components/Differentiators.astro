---
const items = [
  {
    title: 'Trained & Certified Professionals',
    body:  'MNRE-certified engineers handle every project — no subcontracting, no shortcuts.',
    image: '/hero_solar_installation.png',
  },
  {
    title: 'Transparent Process',
    body:  'Upfront pricing, clear timelines, and no hidden charges — from survey to commissioning.',
    image: '/service_commercial.png',
  },
  {
    title: 'No Compromise on Quality',
    body:  'ALMM-listed panels, IEC-certified inverters, and IS 3043-compliant earthing on every system.',
    image: '/service_industrial.png',
  },
  {
    title: 'Assured After-Sales Service',
    body:  'Dedicated support and proactive O&M for the life of your system — not just until installation.',
    image: '/gallery_residential.png',
  },
];
---

<section class="py-14 bg-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

    <div class="text-center mb-10">
      <p class="section-label mb-3">Our Difference</p>
      <h2 class="section-heading">What makes Ermis different?</h2>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-8">
      {items.map((item) => (
        <div class="flex flex-col group cursor-pointer differentiator-card">
          <!-- Image tile -->
          <div class="w-full rounded-2xl overflow-hidden mb-6 transition-all duration-300 ease-out group-hover:-translate-y-1.5 group-hover:shadow-md relative"
               style="aspect-ratio: 4/3;">
            <img
              src={item.image}
              alt={item.title}
              class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500 ease-out"
              loading="lazy"
            />
            <div class="absolute inset-0 bg-neutral-ink/10 group-hover:bg-transparent transition-colors duration-300"></div>
          </div>
          <!-- Text -->
          <h3 class="font-extrabold text-neutral-ink text-xl mb-2 leading-snug group-hover:text-brand-red transition-colors duration-300">{item.title}</h3>
          <p class="text-base text-gray-600 leading-relaxed">{item.body}</p>
        </div>
      ))}
    </div>

  </div>
</section>

<style>
  .differentiator-card {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s cubic-bezier(0.16, 1, 0.3, 1), transform 0.7s cubic-bezier(0.16, 1, 0.3, 1);
  }
  :global(section.revealed) .differentiator-card {
    opacity: 1;
    transform: translateY(0);
  }
  .differentiator-card:nth-child(1) { transition-delay: 100ms; }
  .differentiator-card:nth-child(2) { transition-delay: 200ms; }
  .differentiator-card:nth-child(3) { transition-delay: 300ms; }
  .differentiator-card:nth-child(4) { transition-delay: 400ms; }
</style>
